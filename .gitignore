# Build output
target/

# OS files
.DS_Store
Thumbs.db

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo

# Environment files
.env
.env.*

# Test coverage
coverage/

# Rendered release manifests (make release-manifests) — never checked in, so a
# deployable manifest can only ever come from an explicit, immutable image.
deploy/*.release.yaml

# Misc
*.log
