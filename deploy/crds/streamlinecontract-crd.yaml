# Generated by `streamline-operator --generate-crds`. DO NOT EDIT.
#
# Source of truth: src/crd/contract.rs (`#[kube(...)]` annotations).
# Regenerate with: make generate-crds
# Reconciled by: nothing — schema only, deliberately NOT installed by
# deploy/crds/kustomization.yaml, NOT granted RBAC, and NOT started by the
# operator, so it cannot advertise unsupported behaviour.
# Reason: the Streamline server exposes POST /api/v1/contracts/{validate,apply} taking {contract: {topic, version, schema_id, assertions}, partition, value}; there is no POST /api/v1/contracts and no schema/compatibility/bindTopics contract this CRD could apply
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: streamlinecontracts.streamline.io
spec:
  group: streamline.io
  names:
    categories: []
    kind: StreamlineContract
    plural: streamlinecontracts
    shortNames:
    - slcon
    singular: streamlinecontract
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef
      name: Cluster
      type: string
    - jsonPath: .spec.compatibility
      name: Compatibility
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for ContractSpec via `CustomResource`
        properties:
          spec:
            description: StreamlineContract is the Schema for the streamlinecontracts API.
            properties:
              bindTopics:
                default: []
                description: Optional list of topic name patterns this contract should bind to.
                items:
                  type: string
                type: array
              clusterRef:
                description: Reference to the parent StreamlineCluster.
                type: string
              compatibility:
                default: BACKWARD
                description: Compatibility policy. Defaults to `BACKWARD` server-side when empty.
                enum:
                - BACKWARD
                - FORWARD
                - FULL
                - NONE
                type: string
              schemaJson:
                description: JSON-encoded schema body. Stored verbatim; the broker validates it on register. Kept as a string so the CRD schema does not have to anticipate every Moonshot schema dialect.
                type: string
            required:
            - clusterRef
            - schemaJson
            type: object
          status:
            description: ContractStatus describes the observed state of a contract.
            nullable: true
            properties:
              boundTopics:
                default: []
                description: Topics the broker confirms are bound to this contract.
                items:
                  type: string
                type: array
              conditions:
                default: []
                description: Standard k8s-style conditions.
                items:
                  description: A single status condition.
                  properties:
                    lastTransitionTime:
                      nullable: true
                      type: string
                    message:
                      nullable: true
                      type: string
                    reason:
                      nullable: true
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Last observed message from the broker / reconciler.
                nullable: true
                type: string
              phase:
                default: Pending
                description: Lifecycle phase reported by the controller.
                enum:
                - Pending
                - Registering
                - Active
                - Failed
                - Deleting
                type: string
              registered:
                default: false
                description: Whether the contract is currently registered and active.
                type: boolean
            type: object
        required:
        - spec
        title: StreamlineContract
        type: object
    served: true
    storage: true
    subresources:
      status: {}
