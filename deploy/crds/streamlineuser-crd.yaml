# Generated by `streamline-operator --generate-crds`. DO NOT EDIT.
#
# Source of truth: src/crd/user.rs (`#[kube(...)]` annotations).
# Regenerate with: make generate-crds
# Reconciled by: UserController (installed via deploy/crds/kustomization.yaml).
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: streamlineusers.streamline.io
spec:
  group: streamline.io
  names:
    categories: []
    kind: StreamlineUser
    plural: streamlineusers
    shortNames:
    - slu
    singular: streamlineuser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef
      name: Cluster
      type: string
    - jsonPath: .spec.authentication.type
      name: Auth
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for UserSpec via `CustomResource`
        properties:
          spec:
            description: StreamlineUser is the Schema for the streamlineusers API
            properties:
              authentication:
                description: Authentication configuration
                properties:
                  credentials:
                    description: Password configuration
                    nullable: true
                    properties:
                      secretRef:
                        description: Reference to a secret containing the password
                        nullable: true
                        properties:
                          key:
                            default: password
                            description: Key within the secret containing the password
                            type: string
                          name:
                            description: Name of the secret
                            type: string
                        required:
                        - name
                        type: object
                      value:
                        description: Password value (for simple setups, prefer secretRef)
                        nullable: true
                        type: string
                    type: object
                  type:
                    description: Authentication type (scram-sha-256, scram-sha-512, plain)
                    enum:
                    - scram-sha256
                    - scram-sha512
                    - plain
                    - tls-client-auth
                    type: string
                required:
                - type
                type: object
              authorization:
                default:
                  acls: []
                  roles: []
                  type: simple
                description: Authorization/permissions configuration
                properties:
                  acls:
                    default: []
                    description: ACL rules for the user
                    items:
                      description: ACL rule for the user
                      properties:
                        operations:
                          description: Operations to allow
                          items:
                            description: Permission/operation type
                            enum:
                            - read
                            - write
                            - create
                            - delete
                            - alter
                            - describe
                            - describeConfigs
                            - alterConfigs
                            - idempotentWrite
                            - all
                            type: string
                          type: array
                        patternType:
                          default: literal
                          description: Pattern type (literal, prefixed)
                          enum:
                          - literal
                          - prefixed
                          type: string
                        permission:
                          default: allow
                          description: Permission type (allow or deny)
                          enum:
                          - allow
                          - deny
                          type: string
                        resourceName:
                          description: Resource name (exact or prefixed)
                          type: string
                        resourceType:
                          description: Resource type (topic, group, cluster, transactional-id)
                          enum:
                          - topic
                          - group
                          - cluster
                          - transactionalId
                          type: string
                      required:
                      - operations
                      - resourceName
                      - resourceType
                      type: object
                    type: array
                  roles:
                    default: []
                    description: Role bindings for RBAC
                    items:
                      type: string
                    type: array
                  type:
                    default: simple
                    description: Authorization type
                    enum:
                    - simple
                    - rbac
                    type: string
                type: object
              clusterRef:
                description: Reference to the StreamlineCluster
                type: string
              quotas:
                description: Quotas for the user
                nullable: true
                properties:
                  connectionRate:
                    description: Connection rate limit (connections/sec)
                    format: int32
                    nullable: true
                    type: integer
                  consumerByteRate:
                    description: Consumer byte rate limit (bytes/sec)
                    format: int64
                    nullable: true
                    type: integer
                  producerByteRate:
                    description: Producer byte rate limit (bytes/sec)
                    format: int64
                    nullable: true
                    type: integer
                  requestRate:
                    description: Request rate limit (requests/sec)
                    format: int32
                    nullable: true
                    type: integer
                type: object
            required:
            - authentication
            - clusterRef
            type: object
          status:
            description: Status of the StreamlineUser
            nullable: true
            properties:
              conditions:
                default: []
                description: Conditions representing user state
                items:
                  description: Condition of the user
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned
                      nullable: true
                      type: string
                    message:
                      description: Human-readable message
                      nullable: true
                      type: string
                    reason:
                      description: Reason for the condition
                      nullable: true
                      type: string
                    status:
                      description: Status of the condition (True, False, Unknown)
                      type: string
                    type:
                      description: Type of condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              credentialsSecret:
                description: Secret containing generated credentials
                nullable: true
                type: string
              errorMessage:
                description: Error message if creation failed
                nullable: true
                type: string
              lastUpdated:
                description: Last update timestamp
                nullable: true
                type: string
              observedGeneration:
                description: Last observed generation
                format: int64
                nullable: true
                type: integer
              phase:
                default: Pending
                description: Current phase of the user
                enum:
                - Pending
                - Unsupported
                - Ready
                - Updating
                - Failed
                - Terminating
                type: string
              ready:
                default: false
                description: Whether the user is ready
                type: boolean
              username:
                description: Username in the cluster
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: StreamlineUser
        type: object
    served: true
    storage: true
    subresources:
      status: {}
