# Generated by `streamline-operator --generate-crds`. DO NOT EDIT.
#
# Source of truth: src/crd/branch.rs (`#[kube(...)]` annotations).
# Regenerate with: make generate-crds
# Reconciled by: nothing — schema only, deliberately NOT installed by
# deploy/crds/kustomization.yaml, NOT granted RBAC, and NOT started by the
# operator, so it cannot advertise unsupported behaviour.
# Reason: the Streamline server's branch API is behind the non-default `branches` cargo feature (absent from both `lite` and `full`) and takes {base_topic, name, base_offsets, created_by} with `<base_topic>:<name>` identifiers, which this CRD cannot express
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: streamlinebranches.streamline.io
spec:
  group: streamline.io
  names:
    categories: []
    kind: StreamlineBranch
    plural: streamlinebranches
    shortNames:
    - slb
    singular: streamlinebranch
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef
      name: Cluster
      type: string
    - jsonPath: .spec.parent
      name: Parent
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for BranchSpec via `CustomResource`
        properties:
          spec:
            description: StreamlineBranch is the Schema for the streamlinebranches API.
            properties:
              clusterRef:
                description: Reference to the parent StreamlineCluster (by metadata.name).
                type: string
              description:
                description: Optional human-readable description.
                nullable: true
                type: string
              parent:
                description: Parent branch name. Defaults to the cluster's main branch when empty.
                nullable: true
                type: string
              retention:
                description: Optional retention policy for branch state. When `None`, the cluster default applies.
                nullable: true
                properties:
                  ttlSeconds:
                    default: 0
                    description: TTL in seconds after which the branch may be GC'd. 0 = keep forever.
                    format: int64
                    type: integer
                type: object
            required:
            - clusterRef
            type: object
          status:
            description: BranchStatus describes the observed state of a branch.
            nullable: true
            properties:
              conditions:
                default: []
                description: Standard k8s-style conditions.
                items:
                  description: A single status condition.
                  properties:
                    lastTransitionTime:
                      nullable: true
                      type: string
                    message:
                      nullable: true
                      type: string
                    reason:
                      nullable: true
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              createdAtMs:
                description: Branch creation timestamp (Unix epoch milliseconds), as reported by the broker.
                format: int64
                nullable: true
                type: integer
              message:
                description: Last observed message from the broker / reconciler.
                nullable: true
                type: string
              phase:
                default: Pending
                description: Current lifecycle phase.
                enum:
                - Pending
                - Creating
                - Ready
                - Failed
                - Deleting
                type: string
              ready:
                default: false
                description: Whether the branch is ready for reads.
                type: boolean
            type: object
        required:
        - spec
        title: StreamlineBranch
        type: object
    served: true
    storage: true
    subresources:
      status: {}
