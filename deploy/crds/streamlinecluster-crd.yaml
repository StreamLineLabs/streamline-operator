# Generated by `streamline-operator --generate-crds`. DO NOT EDIT.
#
# Source of truth: src/crd/cluster.rs (`#[kube(...)]` annotations).
# Regenerate with: make generate-crds
# Reconciled by: ClusterController (installed via deploy/crds/kustomization.yaml).
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: streamlineclusters.streamline.io
spec:
  group: streamline.io
  names:
    categories: []
    kind: StreamlineCluster
    plural: streamlineclusters
    shortNames:
    - slc
    singular: streamlinecluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for ClusterSpec via `CustomResource`
        properties:
          spec:
            description: StreamlineCluster is the Schema for the streamlineclusters API
            properties:
              autoscaling:
                description: |-
                  Auto-scaling configuration.

                  Enabling autoscaling is rejected until the operator can bootstrap a real multi-broker cluster. Scaling the current standalone StatefulSet would create independent brokers and make the HPA fight the controller over `spec.replicas`.
                nullable: true
                properties:
                  behavior:
                    description: Scaling behavior configuration
                    nullable: true
                    properties:
                      scaleDown:
                        description: Scale-down behavior
                        nullable: true
                        properties:
                          policies:
                            default: []
                            description: Scaling policies
                            items:
                              description: Individual scaling policy
                              properties:
                                periodSeconds:
                                  description: Period in seconds
                                  format: int32
                                  type: integer
                                type:
                                  description: Policy type (Pods, Percent)
                                  type: string
                                value:
                                  description: Value for the policy
                                  format: int32
                                  type: integer
                              required:
                              - periodSeconds
                              - type
                              - value
                              type: object
                            type: array
                          selectPolicy:
                            description: Select policy (Max, Min, Disabled)
                            nullable: true
                            type: string
                          stabilizationWindowSeconds:
                            description: Stabilization window in seconds
                            format: int32
                            nullable: true
                            type: integer
                        type: object
                      scaleUp:
                        description: Scale-up behavior
                        nullable: true
                        properties:
                          policies:
                            default: []
                            description: Scaling policies
                            items:
                              description: Individual scaling policy
                              properties:
                                periodSeconds:
                                  description: Period in seconds
                                  format: int32
                                  type: integer
                                type:
                                  description: Policy type (Pods, Percent)
                                  type: string
                                value:
                                  description: Value for the policy
                                  format: int32
                                  type: integer
                              required:
                              - periodSeconds
                              - type
                              - value
                              type: object
                            type: array
                          selectPolicy:
                            description: Select policy (Max, Min, Disabled)
                            nullable: true
                            type: string
                          stabilizationWindowSeconds:
                            description: Stabilization window in seconds
                            format: int32
                            nullable: true
                            type: integer
                        type: object
                    type: object
                  enabled:
                    default: false
                    description: Enable auto-scaling
                    type: boolean
                  maxReplicas:
                    default: 10
                    description: Maximum number of replicas
                    format: int32
                    type: integer
                  minReplicas:
                    default: 1
                    description: Minimum number of replicas
                    format: int32
                    type: integer
                  partitionAware:
                    default: false
                    description: Enable partition-aware scaling
                    type: boolean
                  targetCpuUtilization:
                    default: 70
                    description: Target CPU utilization percentage
                    format: int32
                    type: integer
                  targetLagPerPartition:
                    default: 10000
                    description: Target consumer lag per partition
                    format: int64
                    type: integer
                  targetMemoryUtilization:
                    default: 80
                    description: Target memory utilization percentage
                    format: int32
                    type: integer
                  targetMessagesPerSecond:
                    default: 100000
                    description: Target messages per second per broker
                    format: int64
                    type: integer
                type: object
              env:
                default: []
                description: Additional environment variables
                items:
                  description: |-
                    Environment variable

                    Mirrors the shape of a Kubernetes `EnvVar`: exactly one of `value` (a literal) or `valueFrom` (a reference) must be set, and a `valueFrom` block must declare exactly one of the references below. Anything else is rejected by the operator rather than rendered as an empty variable.
                  properties:
                    name:
                      description: Environment variable name
                      type: string
                    value:
                      description: Literal value; mutually exclusive with valueFrom. Use "" for a deliberately empty variable.
                      nullable: true
                      type: string
                    valueFrom:
                      description: Reference the value is read from; mutually exclusive with value
                      nullable: true
                      properties:
                        configMapKeyRef:
                          description: ConfigMap key reference
                          nullable: true
                          properties:
                            key:
                              description: Key within the configmap
                              type: string
                            name:
                              description: Name of the configmap
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        secretKeyRef:
                          description: Secret key reference
                          nullable: true
                          properties:
                            key:
                              description: Key within the secret
                              type: string
                            name:
                              description: Name of the secret
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                  required:
                  - name
                  type: object
                type: array
              httpPort:
                default: 9094
                description: HTTP/metrics port
                format: int32
                type: integer
              image:
                description: |-
                  Container image to use for the Streamline broker.

                  This is the Streamline *server* image, not the operator image.
                type: string
              imagePullPolicy:
                default: IfNotPresent
                description: Image pull policy
                type: string
              kafkaPort:
                default: 9092
                description: Kafka protocol port
                format: int32
                type: integer
              logLevel:
                default: info
                description: Log level for Streamline
                type: string
              metricsEnabled:
                default: true
                description: Enable metrics endpoint
                type: boolean
              nodeSelector:
                additionalProperties:
                  type: string
                default: {}
                description: |-
                  Node selector for pod placement.

                  Applied: rendered onto the pod template as `spec.nodeSelector`.
                type: object
              podAntiAffinity:
                default: false
                description: |-
                  Spread brokers across nodes with pod anti-affinity.

                  Not applied: the operator renders no `affinity` onto the pod template, so `true` is rejected rather than silently dropped. It also buys nothing today — `replicas` is capped at 1, so there is no second pod to spread.
                type: boolean
              rackAwareness:
                description: |-
                  Rack awareness configuration.

                  Not applied: the operator renders no topology spread or rack labelling, so `enabled: true` is rejected rather than silently dropped.
                nullable: true
                properties:
                  enabled:
                    description: Enable rack awareness
                    type: boolean
                  topologyKey:
                    default: topology.kubernetes.io/zone
                    description: Label key to use for rack ID
                    type: string
                required:
                - enabled
                type: object
              raftPort:
                default: 9095
                description: Raft port for cluster communication
                format: int32
                type: integer
              replicas:
                default: 1
                description: |-
                  Number of Streamline broker replicas.

                  Defaults to 1: this operator renders each replica as a standalone broker and does not yet bootstrap raft peers, so a multi-replica value produces independent brokers rather than a quorum. Raise it only when you bootstrap clustering yourself (see README, "Clustering status").
                format: int32
                type: integer
              resources:
                default:
                  limits:
                    cpu: null
                    memory: null
                  requests:
                    cpu: null
                    memory: null
                description: Resource requirements for each broker
                properties:
                  limits:
                    default:
                      cpu: null
                      memory: null
                    description: Resource limits
                    properties:
                      cpu:
                        description: CPU limit/request (e.g., "500m", "2")
                        nullable: true
                        type: string
                      memory:
                        description: Memory limit/request (e.g., "512Mi", "2Gi")
                        nullable: true
                        type: string
                    type: object
                  requests:
                    default:
                      cpu: null
                      memory: null
                    description: Resource requests
                    properties:
                      cpu:
                        description: CPU limit/request (e.g., "500m", "2")
                        nullable: true
                        type: string
                      memory:
                        description: Memory limit/request (e.g., "512Mi", "2Gi")
                        nullable: true
                        type: string
                    type: object
                type: object
              serviceAccountName:
                description: Service account name
                nullable: true
                type: string
              storage:
                default:
                  accessModes:
                  - ReadWriteOnce
                  size: 10Gi
                  storageClassName: null
                description: Storage configuration
                properties:
                  accessModes:
                    default:
                    - ReadWriteOnce
                    description: Access modes for the PVC
                    items:
                      type: string
                    type: array
                  size:
                    default: 10Gi
                    description: Size of the persistent volume (e.g., "10Gi")
                    type: string
                  storageClassName:
                    description: Storage class name for PVCs
                    nullable: true
                    type: string
                type: object
              tls:
                description: TLS configuration
                nullable: true
                properties:
                  caSecretName:
                    description: Secret containing CA certificate for mTLS
                    nullable: true
                    type: string
                  enabled:
                    description: Enable TLS
                    type: boolean
                  insecureSkipVerify:
                    default: false
                    description: Skip TLS certificate verification (development only, NOT recommended for production)
                    type: boolean
                  mtlsEnabled:
                    default: false
                    description: Enable mTLS (mutual TLS)
                    type: boolean
                  secretName:
                    description: Secret containing TLS certificates
                    type: string
                required:
                - enabled
                - secretName
                type: object
              tolerations:
                default: []
                description: |-
                  Tolerations for pod scheduling.

                  Not applied: the operator renders no `tolerations` onto the pod template, so a non-empty list is rejected rather than silently dropped. Use `nodeSelector`, which is rendered.
                items:
                  description: Toleration for pod scheduling
                  properties:
                    effect:
                      description: Effect (NoSchedule, PreferNoSchedule, NoExecute)
                      nullable: true
                      type: string
                    key:
                      description: Taint key
                      nullable: true
                      type: string
                    operator:
                      description: Operator (Exists, Equal)
                      nullable: true
                      type: string
                    tolerationSeconds:
                      description: Toleration seconds for NoExecute
                      format: int64
                      nullable: true
                      type: integer
                    value:
                      description: Taint value
                      nullable: true
                      type: string
                  type: object
                type: array
              updateStrategy:
                default:
                  maxUnavailable: 1
                  type: RollingUpdate
                description: Rolling update strategy
                properties:
                  maxUnavailable:
                    description: Maximum unavailable pods during rolling update
                    format: int32
                    nullable: true
                    type: integer
                  type:
                    default: RollingUpdate
                    description: Update strategy type (RollingUpdate, OnDelete)
                    type: string
                type: object
            type: object
          status:
            description: Status of the StreamlineCluster
            nullable: true
            properties:
              brokerEndpoints:
                default: []
                description: List of broker endpoints
                items:
                  type: string
                type: array
              conditions:
                default: []
                description: Conditions representing cluster state
                items:
                  description: Condition of the cluster
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned
                      nullable: true
                      type: string
                    message:
                      description: Human-readable message
                      nullable: true
                      type: string
                    reason:
                      description: Reason for the condition
                      nullable: true
                      type: string
                    status:
                      description: Status of the condition (True, False, Unknown)
                      type: string
                    type:
                      description: Type of condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              lastUpdated:
                description: Last update timestamp
                nullable: true
                type: string
              leaderId:
                description: Current leader node ID
                format: int32
                nullable: true
                type: integer
              observedGeneration:
                description: Last observed generation
                format: int64
                nullable: true
                type: integer
              phase:
                default: Pending
                description: Current phase of the cluster
                enum:
                - Pending
                - Running
                - Scaling
                - Upgrading
                - Failed
                - Terminating
                type: string
              readyReplicas:
                default: 0
                description: Number of ready replicas
                format: int32
                type: integer
              replicas:
                default: 0
                description: Total number of replicas
                format: int32
                type: integer
            type: object
        required:
        - spec
        title: StreamlineCluster
        type: object
    served: true
    storage: true
    subresources:
      status: {}
