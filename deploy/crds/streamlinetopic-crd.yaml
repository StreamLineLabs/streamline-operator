# Generated by `streamline-operator --generate-crds`. DO NOT EDIT.
#
# Source of truth: src/crd/topic.rs (`#[kube(...)]` annotations).
# Regenerate with: make generate-crds
# Reconciled by: TopicController (installed via deploy/crds/kustomization.yaml).
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: streamlinetopics.streamline.io
spec:
  group: streamline.io
  names:
    categories: []
    kind: StreamlineTopic
    plural: streamlinetopics
    shortNames:
    - slt
    singular: streamlinetopic
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef
      name: Cluster
      type: string
    - jsonPath: .spec.partitions
      name: Partitions
      type: integer
    - jsonPath: .spec.replicationFactor
      name: Replication
      type: integer
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for TopicSpec via `CustomResource`
        properties:
          spec:
            description: StreamlineTopic is the Schema for the streamlinetopics API
            properties:
              clusterRef:
                description: Reference to the StreamlineCluster
                type: string
              compression:
                default:
                  type: producer
                description: |-
                  Compression configuration.

                  Not applied: compression is chosen by the producer, and the topic API accepts no compression setting, so any value other than the default (`producer`) is rejected rather than silently discarded.
                properties:
                  type:
                    default: producer
                    description: Compression type (none, gzip, snappy, lz4, zstd)
                    type: string
                type: object
              config:
                default:
                  custom: {}
                  flushIntervalMs: null
                  flushMessages: null
                  indexIntervalBytes: null
                  maxMessageBytes: null
                  minInsyncReplicas: null
                  segmentBytes: null
                description: |-
                  Topic configuration overrides.

                  Not applied: the Streamline topic API accepts only a name and a partition count, so every override here is rejected by `TopicController` instead of being silently discarded. Leave unset.
                properties:
                  custom:
                    additionalProperties:
                      type: string
                    default: {}
                    description: Additional custom configurations
                    type: object
                  flushIntervalMs:
                    description: Flush interval in milliseconds
                    format: int64
                    nullable: true
                    type: integer
                  flushMessages:
                    description: Flush after N messages
                    format: int64
                    nullable: true
                    type: integer
                  indexIntervalBytes:
                    description: Index interval in bytes
                    format: int64
                    nullable: true
                    type: integer
                  maxMessageBytes:
                    description: Maximum message size in bytes
                    format: int64
                    nullable: true
                    type: integer
                  minInsyncReplicas:
                    description: Minimum in-sync replicas required for writes
                    format: int32
                    nullable: true
                    type: integer
                  segmentBytes:
                    description: Segment size in bytes
                    format: int64
                    nullable: true
                    type: integer
                type: object
              partitions:
                default: 3
                description: Number of partitions for the topic
                format: int32
                type: integer
              replicationFactor:
                default: 1
                description: |-
                  Replication factor for the topic.

                  Defaults to 1: the Streamline topic API creates single-replica topics, and `TopicController` rejects any other value rather than reporting `Ready` for durability the cluster does not provide.
                format: int32
                type: integer
              retention:
                default:
                  cleanupPolicy: delete
                  retentionBytes: -1
                  retentionMs: -1
                description: |-
                  Retention configuration.

                  Not applied: the Streamline topic API exposes no way to set retention on a topic, so any value other than the defaults below is rejected rather than silently discarded. The defaults describe what the broker actually does — it retains every topic indefinitely — so leaving them alone is an accurate description of the result.
                properties:
                  cleanupPolicy:
                    default: delete
                    description: Delete or compact cleanup policy
                    type: string
                  retentionBytes:
                    default: -1
                    description: Maximum size in bytes per partition; `-1` (the default) means unlimited, which is what the broker does.
                    format: int64
                    type: integer
                  retentionMs:
                    default: -1
                    description: |-
                      Retention time in milliseconds; `-1` (the default) means unlimited.

                      Unlimited is the default because the core broker applies no topic configuration and never expires a segment on age. A `604800000` default described a seven-day policy nothing implemented, and since every non-default value is rejected, that made the *only* accepted value the one that was wrong.
                    format: int64
                    type: integer
                type: object
            required:
            - clusterRef
            type: object
          status:
            description: Status of the StreamlineTopic
            nullable: true
            properties:
              conditions:
                default: []
                description: Conditions representing topic state
                items:
                  description: Condition of the topic
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned
                      nullable: true
                      type: string
                    message:
                      description: Human-readable message
                      nullable: true
                      type: string
                    reason:
                      description: Reason for the condition
                      nullable: true
                      type: string
                    status:
                      description: Status of the condition (True, False, Unknown)
                      type: string
                    type:
                      description: Type of condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              errorMessage:
                description: Error message if creation failed
                nullable: true
                type: string
              lastUpdated:
                description: Last update timestamp
                nullable: true
                type: string
              observedGeneration:
                description: Last observed generation
                format: int64
                nullable: true
                type: integer
              partitionAssignments:
                default: []
                description: Partition assignment information
                items:
                  description: Partition assignment information
                  properties:
                    isr:
                      description: In-sync replica broker IDs
                      items:
                        format: int32
                        type: integer
                      type: array
                    leader:
                      description: Leader broker ID
                      format: int32
                      type: integer
                    partition:
                      description: Partition ID
                      format: int32
                      type: integer
                    replicas:
                      description: Replica broker IDs
                      items:
                        format: int32
                        type: integer
                      type: array
                  required:
                  - isr
                  - leader
                  - partition
                  - replicas
                  type: object
                type: array
              partitions:
                default: 0
                description: Actual number of partitions
                format: int32
                type: integer
              phase:
                default: Pending
                description: Current phase of the topic
                enum:
                - Pending
                - Ready
                - Updating
                - Failed
                - Terminating
                type: string
              ready:
                default: false
                description: Whether the topic is ready
                type: boolean
              replicationFactor:
                default: 0
                description: Actual replication factor
                format: int32
                type: integer
            type: object
        required:
        - spec
        title: StreamlineTopic
        type: object
    served: true
    storage: true
    subresources:
      status: {}
