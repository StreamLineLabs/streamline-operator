# Generated by `streamline-operator --generate-crds`. DO NOT EDIT.
#
# Source of truth: src/crd/backup.rs (`#[kube(...)]` annotations).
# Regenerate with: make generate-crds
# Reconciled by: nothing — schema only, deliberately NOT installed by
# deploy/crds/kustomization.yaml, NOT granted RBAC, and NOT started by the
# operator, so it cannot advertise unsupported behaviour.
# Reason: the Streamline server exposes no backup or restore API
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: streamlinebackups.streamline.io
spec:
  group: streamline.io
  names:
    categories: []
    kind: StreamlineBackup
    plural: streamlinebackups
    shortNames: []
    singular: streamlinebackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef
      name: Cluster
      type: string
    - jsonPath: .spec.backupType
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for BackupSpec via `CustomResource`
        properties:
          spec:
            description: Spec for a StreamlineBackup resource
            properties:
              backupType:
                default: Full
                description: Type of backup
                enum:
                - Full
                - Incremental
                - MetadataOnly
                type: string
              clusterRef:
                description: Reference to the StreamlineCluster to back up
                type: string
              compression:
                default: zstd
                description: Compression algorithm for backup data
                type: string
              includeAcls:
                default: true
                description: Include ACLs and user credentials in backup
                type: boolean
              includeConfigs:
                default: true
                description: Include topic configurations
                type: boolean
              includeOffsets:
                default: true
                description: Include consumer group offsets in backup
                type: boolean
              retentionCount:
                default: 5
                description: Maximum number of backups to retain
                format: uint32
                minimum: 0.0
                type: integer
              schedule:
                description: Schedule (cron expression) for recurring backups (empty = one-shot)
                nullable: true
                type: string
              storage:
                description: Storage destination for the backup
                properties:
                  bucket:
                    description: S3-compatible bucket name
                    nullable: true
                    type: string
                  credentialsSecret:
                    description: Secret reference for storage credentials
                    nullable: true
                    type: string
                  endpoint:
                    description: S3-compatible endpoint URL
                    nullable: true
                    type: string
                  prefix:
                    description: Storage prefix/path
                    nullable: true
                    type: string
                  pvcName:
                    description: PVC name for local storage
                    nullable: true
                    type: string
                  region:
                    description: AWS/GCS region
                    nullable: true
                    type: string
                  type:
                    description: Storage type
                    enum:
                    - S3
                    - GCS
                    - AzureBlob
                    - PVC
                    type: string
                required:
                - type
                type: object
              topics:
                default: []
                description: Topics to back up (empty = all topics)
                items:
                  type: string
                type: array
            required:
            - clusterRef
            - storage
            type: object
          status:
            description: Status of a StreamlineBackup resource
            nullable: true
            properties:
              completedBackups:
                default: 0
                description: Number of completed backups
                format: uint32
                minimum: 0.0
                type: integer
              conditions:
                default: []
                description: Conditions
                items:
                  description: Condition for backup status
                  properties:
                    conditionType:
                      description: Type of condition
                      type: string
                    lastTransitionTime:
                      description: Last transition time
                      type: string
                    message:
                      description: Detailed message
                      type: string
                    reason:
                      description: Human-readable reason
                      type: string
                    status:
                      description: Whether the condition is met
                      type: string
                  required:
                  - conditionType
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  type: object
                type: array
              error:
                description: Error message (if phase is Failed)
                nullable: true
                type: string
              lastBackupDurationSecs:
                description: Duration of the last backup in seconds
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              lastBackupSizeBytes:
                description: Size of the last backup in bytes
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              lastBackupTime:
                description: Timestamp when the last backup started
                nullable: true
                type: string
              lastSuccessfulTime:
                description: Timestamp when the last backup completed
                nullable: true
                type: string
              phase:
                description: Current phase of the backup
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                - Expired
                type: string
              topicsBackedUp:
                default: 0
                description: Number of topics backed up
                format: uint32
                minimum: 0.0
                type: integer
            required:
            - phase
            type: object
        required:
        - spec
        title: StreamlineBackup
        type: object
    served: true
    storage: true
    subresources:
      status: {}
