# Generated by `streamline-operator --generate-crds`. DO NOT EDIT.
#
# Source of truth: src/crd/memory.rs (`#[kube(...)]` annotations).
# Regenerate with: make generate-crds
# Reconciled by: nothing — schema only, deliberately NOT installed by
# deploy/crds/kustomization.yaml, NOT granted RBAC, and NOT started by the
# operator, so it cannot advertise unsupported behaviour.
# Reason: the Streamline server's memory API is behind the non-default `agent-memory` cargo feature and is a remember/recall endpoint, not a provisioning API; a controller could only create plain topics and then report memory tiers Ready that nothing implements
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: streamlinememories.streamline.io
spec:
  group: streamline.io
  names:
    categories: []
    kind: StreamlineMemory
    plural: streamlinememories
    shortNames:
    - slm
    singular: streamlinememory
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.agentId
      name: Agent
      type: string
    - jsonPath: .spec.tenant
      name: Tenant
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for MemorySpec via `CustomResource`
        properties:
          spec:
            description: StreamlineMemory is the Schema for the streamlinememories API.
            properties:
              agentId:
                description: Unique agent identifier that owns this memory partition.
                type: string
              clusterRef:
                description: Reference to the parent StreamlineCluster (by metadata.name).
                type: string
              decay:
                description: Memory decay configuration for automatic relevance scoring.
                nullable: true
                properties:
                  halfLifeDays:
                    default: 7.0
                    description: Half-life in days for memory decay scoring.
                    format: double
                    type: number
                  threshold:
                    default: 0.1
                    description: Threshold below which memories are eligible for garbage collection.
                    format: double
                    type: number
                type: object
              encryptionEnabled:
                default: false
                description: Whether to enable encryption at rest for memory topics.
                type: boolean
              tenant:
                description: Tenant or namespace for multi-tenant deployments.
                type: string
              tiers:
                default:
                  episodicRetentionDays: 30
                  proceduralRetentionDays: -1
                  semanticRetentionDays: 365
                description: Memory tier retention configuration.
                properties:
                  episodicRetentionDays:
                    default: 30
                    description: 'Retention in days for episodic (event-based) memories. Default: 30.'
                    format: int64
                    type: integer
                  proceduralRetentionDays:
                    default: -1
                    description: 'Retention in days for procedural (skill/habit) memories. Default: -1 (infinite).'
                    format: int64
                    type: integer
                  semanticRetentionDays:
                    default: 365
                    description: 'Retention in days for semantic (knowledge) memories. Default: 365.'
                    format: int64
                    type: integer
                type: object
            required:
            - agentId
            - clusterRef
            - tenant
            type: object
          status:
            description: MemoryStatus describes the observed state of an agent's memory.
            nullable: true
            properties:
              conditions:
                default: []
                description: Standard k8s-style conditions.
                items:
                  description: A single status condition.
                  properties:
                    lastTransitionTime:
                      nullable: true
                      type: string
                    message:
                      nullable: true
                      type: string
                    reason:
                      nullable: true
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              episodicEventCount:
                default: 0
                description: Number of episodic memory events stored.
                format: int64
                type: integer
              message:
                description: Last observed message from the reconciler.
                nullable: true
                type: string
              phase:
                default: Pending
                description: Current lifecycle phase.
                enum:
                - Pending
                - Provisioning
                - Ready
                - Decaying
                - Failed
                - Deleting
                type: string
              proceduralEventCount:
                default: 0
                description: Number of procedural memory entries stored.
                format: int64
                type: integer
              ready:
                default: false
                description: Whether the memory topics are ready.
                type: boolean
              semanticEventCount:
                default: 0
                description: Number of semantic memory entries stored.
                format: int64
                type: integer
            type: object
        required:
        - spec
        title: StreamlineMemory
        type: object
    served: true
    storage: true
    subresources:
      status: {}
